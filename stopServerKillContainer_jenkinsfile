node {
	def rootDir = pwd()
    def build = load("${rootDir}@script/build.groovy")

    stage("stop server") {
        build.stopServerIfItRun()
    }
    stage("kill container") {
        build.killLastContainer()
    }
}